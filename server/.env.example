# Supabase 配置
# 推荐使用 service_role key（无 RLS 限制）
# 也可以直接用 VITE_ 前缀，与根目录 .env 保持一致
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-service-role-key

# 如果直接用根目录 .env 的值，可以保留 VITE_ 前缀
# SUPABASE_URL 会自动读取 VITE_SUPABASE_URL
# SUPABASE_KEY 会自动读取 VITE_SUPABASE_PUBLISHABLE_KEY
